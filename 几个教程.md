# 声卡
https://cloud.tencent.com/developer/ask/sof/106428164
sudo apt-get install -y pulseaudio alsa-utils
RUN apt-get install -y pulseaudio alsa-utils
docker run -t -i -e "PULSE_SERVER=${PULSE_SERVER}" -v /mnt/wslg/:/mnt/wslg/ image_name
# 显卡
https://blog.csdn.net/m0_63070489/article/details/145798161
在WSL2中 安装native docker, + 在docker 中启用 gpu（要先在win上配好CUDA等）
nvidia-smi
安装NVIDIA Container Toolkit
验证：docker run --rm --gpus all nvidia/cuda:12.6.3-base-ubuntu24.04 nvidia-smi  （可能还需要配置环境变量，待测试）
# 重置用户密码
wsl.exe --user root
passwd root
passwd username 